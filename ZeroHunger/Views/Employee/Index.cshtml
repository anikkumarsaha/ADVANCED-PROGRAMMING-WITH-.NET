
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>


<table class="table table-bordered text-center">
    <tr class="bg-info">
        <td>Name</td>
        <td>Operation</td>
    </tr>

    @foreach (var cs in Model)
    {
        <tr>
            <td>@cs.Name</td>
            <td><a href="/Employee/ShowAssignedTasks/@cs.Id" class="btn btn-success">Login as @cs.Name</a></td>
        </tr>
    }
</table>